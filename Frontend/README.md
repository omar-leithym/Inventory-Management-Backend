# FreshFlow - Inventory Management Frontend

Run dev server:

```bash
npm run dev
```

Dev server defaults to port 3001 (or next available port).

The project uses MUI 5, Recharts, Axios, and React Router 6. Mock data is in `src/mocks`.
